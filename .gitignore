# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Local environment / secrets (this repo ships no code, but keep this as a backstop)
.env
.env.local

# Claude Code local settings (personal, not part of the shared template)
.claude/settings.local.json
